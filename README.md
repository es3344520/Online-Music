在线播放器，把音乐存储在cloudflare R2  把网页播放器部署在Vercel  

为了快速访问，最好是有自己的域名  

在Vercel填写好R2相关的账号密钥接口  

Vercel相关的环境变量命名参考：api/music.js进行配置，不懂就问AI。R2有4个令牌是必须的，R2域名自定义之后可以填写， 

 注意：配置R2的时候不要开启公开访问，项目都配置好了，不需要开启也可以获取音频文件，为了防止链接盗用，每一个链接都有签名，且有过期时间，默认有效10分钟，你可以在api/music.js进行配置，不懂就问AI。
